{
    "name": "memkit",
    "version": "0.1.0",
    "description": "Local persistent memory layer for AI agents — MCP server + AI SDK adapter",
    "module": "src/server.ts",
    "bin": {
        "code-memory": "./src/server.ts"
    },
    "scripts": {
        "start": "bun run src/server.ts",
        "dev": "bun --watch src/server.ts"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^1.12.1",
        "ai": "^6.0.134",
        "zod": "^3.24.4"
    },
    "devDependencies": {
        "@types/bun": "latest"
    }
}
